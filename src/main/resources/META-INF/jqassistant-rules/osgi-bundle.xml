<jqassistant-rules xmlns="http://schema.jqassistant.org/rule/v2.9"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://schema.jqassistant.org/rule/v2.9 https://schema.jqassistant.org/rule/jqassistant-rule-v2.2.xsd">

    <group id="osgi:Default">
        <description>Core OSGi bundle concepts: identifies bundles, extracts metadata (version, name, vendor, activator, activation policy, execution environment), resolves fragments and Require-Bundle dependencies.</description>
        <includeConcept refId="osgi:Bundle"/>
        <includeConcept refId="osgi:BundleVersion"/>
        <includeConcept refId="osgi:BundleName"/>
        <includeConcept refId="osgi:BundleVendor"/>
        <includeConcept refId="osgi:SingletonBundle"/>
        <includeConcept refId="osgi:BundleActivator"/>
        <includeConcept refId="osgi:LazyActivation"/>
        <includeConcept refId="osgi:ExecutionEnvironment"/>
        <includeConcept refId="osgi:Fragment"/>
        <includeConcept refId="osgi:FragmentResolvesToHost"/>
        <includeConcept refId="osgi:RequiresBundle"/>
        <includeConcept refId="osgi:RequiredBundleResolution"/>
        <includeConcept refId="osgi:BundleDependency"/>
    </group>

    <!-- Mark JARs with Bundle-SymbolicName as OSGi Bundles -->
    <concept id="osgi:Bundle">
        <description>Labels JAR archives containing a Bundle-SymbolicName manifest header as :Osgi:Bundle and sets the bundleSymbolicName property.</description>
        <cypher><![CDATA[
            MATCH (jar:Jar:Archive)-[:CONTAINS]->(mf:Manifest:File)
                  -[:DECLARES]->(ms:ManifestSection {name: "Main"})
                  -[:HAS]->(entry:ManifestEntry)
            WHERE entry.name = "Bundle-SymbolicName"
            WITH jar, CASE
                WHEN entry.value CONTAINS ";" THEN trim(split(entry.value, ";")[0])
                ELSE trim(entry.value)
            END AS symbolicName
            SET jar:Osgi:Bundle, jar.bundleSymbolicName = symbolicName
            RETURN jar.bundleSymbolicName AS BundleSymbolicName
        ]]></cypher>
    </concept>

    <!-- Extract Bundle-Version -->
    <concept id="osgi:BundleVersion">
        <requiresConcept refId="osgi:Bundle"/>
        <description>Sets the bundleVersion property on OSGi bundles from the Bundle-Version manifest header.</description>
        <cypher><![CDATA[
            MATCH (bundle:Osgi:Bundle)-[:CONTAINS]->(mf:Manifest:File)
                  -[:DECLARES]->(ms:ManifestSection {name: "Main"})
                  -[:HAS]->(entry:ManifestEntry {name: "Bundle-Version"})
            SET bundle.bundleVersion = trim(entry.value)
            RETURN bundle.bundleSymbolicName AS BundleSymbolicName, bundle.bundleVersion AS Version
        ]]></cypher>
    </concept>

    <!-- Extract Bundle-Name -->
    <concept id="osgi:BundleName">
        <requiresConcept refId="osgi:Bundle"/>
        <description>Sets the bundleName property on OSGi bundles from the Bundle-Name manifest header.</description>
        <cypher><![CDATA[
            MATCH (bundle:Osgi:Bundle)-[:CONTAINS]->(mf:Manifest:File)
                  -[:DECLARES]->(ms:ManifestSection {name: "Main"})
                  -[:HAS]->(entry:ManifestEntry {name: "Bundle-Name"})
            SET bundle.bundleName = trim(entry.value)
            RETURN bundle.bundleSymbolicName AS BundleSymbolicName, bundle.bundleName AS Name
        ]]></cypher>
    </concept>

    <!-- Extract Bundle-Vendor -->
    <concept id="osgi:BundleVendor">
        <requiresConcept refId="osgi:Bundle"/>
        <description>Sets the bundleVendor property on OSGi bundles from the Bundle-Vendor manifest header.</description>
        <cypher><![CDATA[
            MATCH (bundle:Osgi:Bundle)-[:CONTAINS]->(mf:Manifest:File)
                  -[:DECLARES]->(ms:ManifestSection {name: "Main"})
                  -[:HAS]->(entry:ManifestEntry {name: "Bundle-Vendor"})
            SET bundle.bundleVendor = trim(entry.value)
            RETURN bundle.bundleSymbolicName AS BundleSymbolicName, bundle.bundleVendor AS Vendor
        ]]></cypher>
    </concept>

    <!-- Detect singleton bundles -->
    <concept id="osgi:SingletonBundle">
        <requiresConcept refId="osgi:Bundle"/>
        <description>Sets the singleton property to true on OSGi bundles declared as singleton in the Bundle-SymbolicName header.</description>
        <cypher><![CDATA[
            MATCH (bundle:Osgi:Bundle)-[:CONTAINS]->(mf:Manifest:File)
                  -[:DECLARES]->(ms:ManifestSection {name: "Main"})
                  -[:HAS]->(entry:ManifestEntry {name: "Bundle-SymbolicName"})
            WHERE toLower(entry.value) CONTAINS "singleton:=true"
            SET bundle.singleton = true
            RETURN bundle.bundleSymbolicName AS SingletonBundle
        ]]></cypher>
    </concept>

    <!-- Extract Bundle-Activator class reference -->
    <concept id="osgi:BundleActivator">
        <requiresConcept refId="osgi:Bundle"/>
        <description>Sets the bundleActivator property and creates a relationship from the bundle to its activator class.</description>
        <cypher><![CDATA[
            MATCH (bundle:Osgi:Bundle)-[:CONTAINS]->(mf:Manifest:File)
                  -[:DECLARES]->(ms:ManifestSection {name: "Main"})
                  -[:HAS]->(entry:ManifestEntry {name: "Bundle-Activator"})
            SET bundle.bundleActivator = trim(entry.value)
            RETURN bundle.bundleSymbolicName AS BundleSymbolicName, bundle.bundleActivator AS Activator
        ]]></cypher>
    </concept>

    <!-- Detect lazy activation policy -->
    <concept id="osgi:LazyActivation">
        <requiresConcept refId="osgi:Bundle"/>
        <description>Sets the activationPolicy property on bundles with Bundle-ActivationPolicy: lazy.</description>
        <cypher><![CDATA[
            MATCH (bundle:Osgi:Bundle)-[:CONTAINS]->(mf:Manifest:File)
                  -[:DECLARES]->(ms:ManifestSection {name: "Main"})
                  -[:HAS]->(entry:ManifestEntry {name: "Bundle-ActivationPolicy"})
            SET bundle.activationPolicy = trim(entry.value)
            RETURN bundle.bundleSymbolicName AS BundleSymbolicName, bundle.activationPolicy AS ActivationPolicy
        ]]></cypher>
    </concept>

    <!-- Extract Bundle-RequiredExecutionEnvironment -->
    <concept id="osgi:ExecutionEnvironment">
        <requiresConcept refId="osgi:Bundle"/>
        <description>Sets the requiredExecutionEnvironment property from the Bundle-RequiredExecutionEnvironment manifest header.</description>
        <cypher><![CDATA[
            MATCH (bundle:Osgi:Bundle)-[:CONTAINS]->(mf:Manifest:File)
                  -[:DECLARES]->(ms:ManifestSection {name: "Main"})
                  -[:HAS]->(entry:ManifestEntry {name: "Bundle-RequiredExecutionEnvironment"})
            SET bundle.requiredExecutionEnvironment = trim(entry.value)
            RETURN bundle.bundleSymbolicName AS BundleSymbolicName, bundle.requiredExecutionEnvironment AS ExecutionEnvironment
        ]]></cypher>
    </concept>

    <!-- Identify fragment bundles -->
    <concept id="osgi:Fragment">
        <requiresConcept refId="osgi:Bundle"/>
        <description>Labels bundles with a Fragment-Host header as :Osgi:Fragment and extracts the host bundle symbolic name.</description>
        <cypher><![CDATA[
            MATCH (bundle:Osgi:Bundle)-[:CONTAINS]->(mf:Manifest:File)
                  -[:DECLARES]->(ms:ManifestSection {name: "Main"})
                  -[:HAS]->(entry:ManifestEntry {name: "Fragment-Host"})
            WITH bundle, CASE
                WHEN entry.value CONTAINS ";" THEN trim(split(entry.value, ";")[0])
                ELSE trim(entry.value)
            END AS hostSymbolicName,
            entry.value AS rawValue
            SET bundle:Osgi:Fragment,
                bundle.fragmentHostSymbolicName = hostSymbolicName,
                bundle.fragmentHostRaw = rawValue
            RETURN bundle.bundleSymbolicName AS Fragment, hostSymbolicName AS FragmentHost
        ]]></cypher>
    </concept>

    <!-- Resolve fragment-to-host relationships -->
    <concept id="osgi:FragmentResolvesToHost">
        <requiresConcept refId="osgi:Fragment"/>
        <description>Creates FRAGMENT_OF relationships from fragment bundles to their host bundles (when the host exists in the graph).</description>
        <cypher><![CDATA[
            MATCH (fragment:Osgi:Fragment), (host:Osgi:Bundle)
            WHERE fragment.fragmentHostSymbolicName = host.bundleSymbolicName
              AND NOT (fragment)-[:FRAGMENT_OF]->(host)
            CREATE (fragment)-[:FRAGMENT_OF]->(host)
            RETURN fragment.bundleSymbolicName AS Fragment, host.bundleSymbolicName AS Host
        ]]></cypher>
    </concept>

    <!-- Parse Require-Bundle headers into relationships (Java-based for proper quote-aware parsing) -->
    <concept id="osgi:RequiresBundle">
        <requiresConcept refId="osgi:Bundle"/>
        <description>Parses the Require-Bundle manifest header and creates REQUIRES_BUNDLE relationships between bundles (as intermediate nodes with symbolicName and versionRange).</description>
        <source language="osgi"><![CDATA[parseRequireBundle]]></source>
    </concept>

    <!-- Create bundle-level dependency relationships -->
    <concept id="osgi:BundleDependency">
        <requiresConcept refId="osgi:BundleVersion"/>
        <requiresConcept refId="osgi:RequiredBundleResolution"/>
        <requiresConcept refId="osgi:PackageWiring"/>
        <description>Creates DEPENDS_ON relationships between bundles based on Require-Bundle resolution and Import/Export package wiring. Properties viaRequireBundle and viaPackageWiring indicate the dependency source.</description>
        <cypher><![CDATA[
            // Collect all dependency pairs by symbolic name
            OPTIONAL MATCH (s1:Osgi:Bundle)-[:REQUIRES_BUNDLE]->(:Osgi:RequiredBundle)-[:RESOLVES_TO]->(t1:Osgi:Bundle)
            WHERE s1.bundleSymbolicName <> t1.bundleSymbolicName
            WITH collect(DISTINCT [s1.bundleSymbolicName, t1.bundleSymbolicName]) AS rbPairs
            OPTIONAL MATCH (s2:Osgi:Bundle)-[:IMPORTS_PACKAGE]->(:Osgi:ImportedPackage)<-[:SATISFIES]-(:Osgi:ExportedPackage)<-[:EXPORTS_PACKAGE]-(t2:Osgi:Bundle)
            WHERE s2.bundleSymbolicName <> t2.bundleSymbolicName
            WITH rbPairs, collect(DISTINCT [s2.bundleSymbolicName, t2.bundleSymbolicName]) AS pkgPairs
            // Combine into unique pairs
            WITH [p IN rbPairs + pkgPairs WHERE p[0] IS NOT NULL | p] AS allPairs
            UNWIND allPairs AS pair
            WITH DISTINCT pair[0] AS sourceName, pair[1] AS targetName
            // Determine dependency types
            WITH sourceName, targetName,
              EXISTS { MATCH (s:Osgi:Bundle {bundleSymbolicName: sourceName})-[:REQUIRES_BUNDLE]->(:Osgi:RequiredBundle)-[:RESOLVES_TO]->(t:Osgi:Bundle {bundleSymbolicName: targetName}) } AS viaRequireBundle,
              EXISTS { MATCH (s:Osgi:Bundle {bundleSymbolicName: sourceName})-[:IMPORTS_PACKAGE]->(:Osgi:ImportedPackage)<-[:SATISFIES]-(:Osgi:ExportedPackage)<-[:EXPORTS_PACKAGE]-(t:Osgi:Bundle {bundleSymbolicName: targetName}) } AS viaPackageWiring
            // Pick representative nodes and create relationship
            MATCH (source:Osgi:Bundle {bundleSymbolicName: sourceName})
            WITH sourceName, targetName, viaRequireBundle, viaPackageWiring, min(id(source)) AS sourceId
            MATCH (target:Osgi:Bundle {bundleSymbolicName: targetName})
            WITH sourceId, targetName, viaRequireBundle, viaPackageWiring, min(id(target)) AS targetId
            MATCH (source:Osgi:Bundle) WHERE id(source) = sourceId
            MATCH (target:Osgi:Bundle) WHERE id(target) = targetId
            MERGE (source)-[d:DEPENDS_ON]->(target)
            SET d.viaRequireBundle = viaRequireBundle, d.viaPackageWiring = viaPackageWiring
            RETURN source.bundleSymbolicName AS Source, target.bundleSymbolicName AS Target, d.viaRequireBundle AS ViaRequireBundle, d.viaPackageWiring AS ViaPackageWiring
            ORDER BY Source, Target
        ]]></cypher>
    </concept>

    <!-- Resolve Require-Bundle to actual bundles -->
    <concept id="osgi:RequiredBundleResolution">
        <requiresConcept refId="osgi:RequiresBundle"/>
        <description>Creates RESOLVES_TO relationships from RequiredBundle nodes to the actual Bundle they refer to (when present in the graph).</description>
        <cypher><![CDATA[
            MATCH (rb:Osgi:RequiredBundle), (target:Osgi:Bundle)
            WHERE rb.symbolicName = target.bundleSymbolicName
              AND NOT (rb)-[:RESOLVES_TO]->(target)
            CREATE (rb)-[:RESOLVES_TO]->(target)
            RETURN rb.symbolicName AS RequiredBundle, target.bundleSymbolicName AS ResolvedTo
        ]]></cypher>
    </concept>

</jqassistant-rules>
